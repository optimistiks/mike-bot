# Put Vercel and BotFather secrets in place

Type: task
Status: open
Blocked by: 06, 07

## Question

Create the Vercel project for this repo's `v2` branch, set `BOT_TOKEN` (already issued) and the other env vars the research tickets name (webhook secret, database URL, Mini App URL). Point BotFather Mini App / Menu Button at the Vercel URL. Record where secrets live; do not paste token values into the ticket.
